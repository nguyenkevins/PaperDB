[package]
name = "paperdb-build"
version = "0.1.0"
edition = "2021"

[dependencies]
paperdb-core = { path = "../paperdb-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
bincode = "1"
rand = "0.8"
